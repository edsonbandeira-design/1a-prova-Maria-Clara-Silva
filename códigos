# Sistema escolar: calcula média anual e verifica aprovação

# Lista para armazenar as 8 notas
notas = []

# Coletando notas do usuário
for i in range(1, 9):
    while True:
        try:
            nota = float(input(f"Digite a nota da prova {i} (0 a 10): "))
            if 0 <= nota <= 10:
                notas.append(nota)
                break
            else:
                print("Nota inválida! Digite um valor entre 0 e 10.")
        except ValueError:
            print("Entrada inválida! Digite um número.")

# Dividindo por semestre
semestre1 = notas[:4]
semestre2 = notas[4:]

media1 = sum(semestre1)/4
media2 = sum(semestre2)/4
media_anual = sum(notas)/8

print("\n=== RESULTADOS ===")
print(f"Média do 1º semestre: {media1:.2f}")
print(f"Média do 2º semestre: {media2:.2f}")
print(f"Média anual: {media_anual:.2f}")

if media_anual >= 7:
    print("Parabéns! O aluno foi APROVADO.")
else:
    print("Infelizmente, o aluno foi REPROVADO.")

# Termômetro: converte temperatura entre Celsius e Fahrenheit

temp = float(input("Digite a temperatura: "))
unidade = input("Digite a unidade da temperatura (C para Celsius, F para Fahrenheit): ").upper()

if unidade == "C":
    fahrenheit = (temp * 9/5) + 32
    print(f"{temp:.2f}°C equivalem a {fahrenheit:.2f}°F")
elif unidade == "F":
    celsius = (temp - 32) * 5/9
    print(f"{temp:.2f}°F equivalem a {celsius:.2f}°C")
else:
    print("Unidade inválida! Digite 'C' ou 'F'.")
